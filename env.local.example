# Copy this file to .env (do not commit .env)

# App
NEXT_PUBLIC_APP_URL=http://localhost:4400
NEXT_PUBLIC_BACKSTAGE_API_URL=http://localhost:4402
PORT=4400
NODE_ENV=development

# Database (docker-compose services)
DATABASE_URL=postgresql://postgres:postgres@db:5432/idp_wrapper

# Redis
REDIS_URL=redis://redis:6379

# Auth (dev only; replace for real usage)
JWT_SECRET=dev_jwt_secret_change_me
NEXTAUTH_SECRET=dev_nextauth_secret_change_me

# Backstage integration (mock/real URL)
BACKSTAGE_API_URL=http://localhost:4402
